# Рамазан — AI Agent и Backend

## Зона ответственности

`backend/app/main.py`, `backend/app/schemas.py`, оркестрация агента, инструменты, хранение инцидентов и work orders. Стыки — `docs/API_CONTRACT.md` и функции Абыла в `backend/app/services/`.

## Сейчас

- FastAPI маршруты и схемы запросов созданы.
- `analysis_service.py` пока возвращает demo-ответ; вызовов OpenAI нет.
- `action_service.py` проверяет вариант и бюджет, но хранит номер заказа только в памяти процесса.

## Дальше

1. Получить от Абыла реальные функции чтения жалоб и расчётов.
2. Добавить OpenAI tool calling с ограниченным набором инструментов; числа брать только из Python.
3. Сохранить формат ответа `/analyze`, `/simulate`, `/action` по контракту.
4. В `agent_steps` перечислять только реально выполненные вызовы/действия.

## Журнал изменений

После каждой своей сессии добавляй дату, изменённые файлы, результат, проверку и следующий шаг. Обновляй также `STATUS.md` и общий `README.md`, если меняется общая картина.
