# Абыл — данные и расчёты

## Зона ответственности

`backend/data/` и функции данных/симуляции в `backend/app/services/`. Возвращаемые поля согласуются с `docs/API_CONTRACT.md`.

## Сейчас

- Добавлен `backend/scripts/generate_demo_data.py`: он создаёт 4 200 жалоб, 35 вышек и население 12 районов.
- Данные Tower #17, #23, #31 и #8 сохраняют отдельные демонстрационные состояния.
- Сервис данных предоставляет фильтрацию жалоб/вышек, чтение населения/инцидентов и поиск ближайшей вышки.
- Симуляция бюджета считает прогнозную нагрузку и оценку затронутых пользователей; формулы и ограничения записаны в API-контракте.
- Недавние демонстрационные жалобы используют `demo_age_minutes`, чтобы примерный кластер сохранялся при повторном запуске через несколько часов или дней.

## Дальше

1. Подключить текущий контракт к сквозному сценарию с Рамазаном и Мансом.
2. При необходимости согласовать с командой формулу оценки охвата и прироста покрытия.
3. Проверить сценарии запуска backend и `POST /simulate` в окружении с установленными зависимостями.

## Журнал изменений

- 2026-09-23: добавлены генератор данных, 4 200 жалоб, 35 вышек, население 12 районов, фильтрация и поиск ближайшей вышки; временная формула симуляции заменена оценочной с ограничением по затронутым пользователям. Генератор выполнен. Автоматические проверки пока не запускались.

После каждой своей сессии добавляй дату, изменённые файлы, результат, проверку и следующий шаг. Обновляй также `STATUS.md` и API-контракт, если меняются поля.
