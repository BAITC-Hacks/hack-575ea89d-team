# Абыл — данные и расчёты

## Зона ответственности

`backend/data/` и функции данных/симуляции в `backend/app/services/`. Возвращаемые поля согласуются с `docs/API_CONTRACT.md`.

## Сейчас

- Есть только несколько синтетических вышек, инцидентов и три варианта решения.
- `complaints.json` и `population.json` пусты.
- Формула симуляции временная и помечена в коде.

## Дальше

1. Создать 3–5 тысяч связанных жалоб и 30–50 вышек; Tower #17 должна дать демонстрационный кластер.
2. Добавить функции чтения/фильтрации жалоб, сетевых данных, населения и поиска вышки.
3. Реализовать расчёт затронутых пользователей, вариантов и бюджета; описать формулы и допущения.
4. Передать Рамазану имена функций и примеры JSON-ответов.

## Журнал изменений

После каждой своей сессии добавляй дату, изменённые файлы, результат, проверку и следующий шаг. Обновляй также `STATUS.md` и API-контракт, если меняются поля.
