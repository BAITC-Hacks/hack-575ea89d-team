# Проверка адаптивного агента — ноутбук 1, 2026-09-23

Основа: main `58c794b`, ветка `work/laptop-1-agent`. Отдельная копия репозитория и `.venv`, Python 3.12, numpy 2.5.3, pandas 2.3.3. Прежняя локальная копия не изменена. `research/`, `tools/`, `demo/`, тесты напарника и материалы организатора не менялись.

## Итог после интеграции PR #5/#6 (общая v4)

Research `3714cf9` и demo `5e41795` проверены и включены через merge без перезаписи их веток. Контракт v1 сохранён; research source не изменён. В демо исправлена универсальная фраза про отсутствие пересечений, в pitch обновлены фактические цифры. Независимая валидация напарника в docs/VALIDATION.md остаётся снимком baseline.

Прямое подключение нового research к v3 снизило медиану seed 0–9 до **3 013 056** (seed 42 **2 925 293**). Обнаружен пробел planner: после начальных гипотез он проверял повтор/канал, но не альтернативный тариф той же аудитории. В v4, если ни один измеренный вариант аудитории не имеет положительной нижней оценки, исследуется следующий кандидат research для той же аудитории. Его приоритет оценивается через типичный наблюдаемый lift и неопределённость, а финальный выбор требует прямого пилота. Новых предположений о скрытых эффектах нет. Отдельный искусственный тест проверяет такой переход к альтернативе.

| Общая v4 | baseline | интеграция |
|---|---:|---:|
| seed 42 net | 2 519 731 | **3 480 530** |
| seed 0–9 median | 3 178 575 | **3 510 288** |
| seed 0–9 min | 1 882 072 | **2 282 692** |
| seed 0–9 max | 3 727 614 | **4 266 131** |
| Новые seed 30–39 median | 3 266 384 | **3 776 260** |
| Новые seed 30–39 min | 2 324 309 | **1 774 321** |

На seed 0–9 улучшение в **6/10**, ухудшение в **4/10** парах (seed 2,6,7,9); на новых seed 30–39 улучшение **7/10**, ухудшение **3/10** (30,31,32). Особо заметное ухудшение seed 32: **3 726 158 → 1 774 321**. Все прогоны положительны, но минимальный результат контрольной серии стал хуже. Полные значения: [integrated-holdout.json](agent_evidence/integrated-holdout.json); стандартные прогоны: [integrated-v4-10.txt](agent_evidence/integrated-v4-10.txt). Константы по контрольной серии не менялись.

Общая v4 имеет меньшую медиану seed 0–9, чем v3 со старым research (**3 724 743**). Интеграция выбрана ради корректной обработки пустой истории, выбросов, больших сегментов и использования альтернативных гипотез. Это **компромисс, а не доказательство улучшения ожидаемого результата относительно v3**. v3 сохранена отдельным коммитом `5a94ccc`; скрытый результат обеих неизвестен. Не проводился дальнейший подбор параметров по mock для устранения отдельных проигрышей.

Seed 42: **20 пилотов + 10 финальных кампаний**, **9 037 контактов**, **28 040** общих расходов (пилоты **11 376**, финальные **16 664**); gross **3 508 570**, net **3 480 530**. Один разведочный пилот имел отрицательный истинный mock-эффект; он учтён в результате, не исключён. Финальные каналы push и SMS. Самый долгий полный evaluate_agent в новой серии — **3,84 с**.

После слияния повторены local_eval, --runs 10, make_submission дважды (побайтовое совпадение), tools/report. **17/17 общих тестов и 6/6 независимых тестов стратегии прошли**; SHA256 исходных материалов совпадают. Итоговый CSV SHA256: `28d43b4bb39e666eac4483ddbb0496dcf9ded3643652db746c06b9e06c8ecd11`. Отчёт [integrated-tests.txt](agent_evidence/integrated-tests.txt). Демо: `node --check demo/app.js` и исполнение render на реальном JSON подтвердили 20 строк пилотов/10 кампаний и отказ неверной версии схемы. Визуальная проверка браузером **не выполнена**: локальный URL заблокирован `ERR_BLOCKED_BY_CLIENT`.

Ниже сохранён журнал **до интеграции**; числа v1–v3 и прежний hash CSV не относятся к итоговой v4.

## Сравнение на одинаковых seed

Все суммы — **локальная учебная среда**, не судейский балл. Сырые журналы находятся в [agent_evidence](agent_evidence/). Сравнения используют тот же официальный evaluate_agent, baseline восстановлен из planner исходного main; seed и данные совпадают. При разных действиях агента seed не гарантирует одинаковую подвыборку пилота — это парные запуски, а не идентичные наблюдения.

| Версия | seed 42, net | seed 0–9, медиана | минимум | максимум | В плюс |
|---|---:|---:|---:|---:|---:|
| Baseline, 12 SMS × 150 | 2 519 731 | 3 178 575 | 1 882 072 | 3 727 614 | 10/10 |
| v1, короткая разведка, строгая нижняя оценка | 2 022 310 | 2 385 840 | 1 567 304 | 3 093 186 | 10/10 |
| v2, более широкая разведка | 2 344 756 | 2 692 759 | 1 586 444 | 3 752 536 | 10/10 |
| Итоговая v3, ожидаемая чистая выручка | 3 307 626 | 3 724 743 | 2 787 794 | 4 365 941 | 10/10 |

v1/v2 ухудшили медиану: неопределённость больших аудиторий отвлекала пилоты, а требование положительной нижней оценки исключало умеренно прибыльные варианты. В v2 шире начальное покрытие, размеры зависят от ARPU, ценность повторного замера учитывает ожидаемое уменьшение ошибки. В v3 разведка сохранилась, но финальный план использует ожидаемую выручку со слабым сжатием к нулю, без порога статистической значимости. Не производился перебор констант по seed или извлечение скрытых эффектов. Это три последовательных изменения правил, не независимое подтверждение обобщения.

После фиксации v3 проведено дополнительное сравнение на ранее не использованных seed **20–29**: медиана baseline **3 148 564**, v3 **3 741 815**; улучшение во всех 10 парах. Самый долгий полный evaluate_agent в этой серии — **1,09 секунды** (с округлением вверх). Точные метрики/время каждого запуска — [holdout.json](agent_evidence/holdout.json). Новые параметры по этой серии не выбирались.

Seed 42: 20 пилотов, 10 финальных кампаний, 13 467 контактов суммарно, затраты 61 176 из 100 000. Пилоты расходовали 20 000, финальные кампании — 41 176. Финальные каналы push и SMS; в разведке также реально проверялись digital_ads и call.

## Реально выполнено

- `python local_eval.py`: PASS, без отброшенных кампаний.
- `python local_eval.py --runs 10`: 10/10 положительных, парное сравнение выше.
- `python -m unittest discover -s tests -v`: 8/8; включает SHA256 исходных файлов организатора, ресурсы, влияние пилотов и совпадение submission.
- `python -m unittest strategy.test_planner -v`: 5/5. Искусственные эффекты без мока: повтор неопределённых наблюдений и ранняя остановка, изменение канала по результату пилота, пересечения, бюджет/контакты/количество, ограничение попыток при отказе API.
- `python make_submission.py` дважды, побайтовое сравнение одинаково. SHA256 CSV: `f6db10c6585aefdee95342de702dfe2e4c9068fb9a1b754d1d0661f885e07789`.

## Как устроены решения и ограничения

1. До 12 начальных гипотез в порядке research. Размер растёт как квадратный корень отношения ARPU сегмента к медианному ARPU первых гипотез, в пределах 80–200; ресурсные остатки могут уменьшить его до 10. Исторические частоты не используются как конверсия.
2. Рабочая ошибка: `sqrt(max(1, observed_repeat_variance) / n)`. Это консервативная эвристика, **не калиброванный доверительный интервал** и не поправка на множественный отбор. Повтор выбирается для неопределённого знака/конкуренции; на одну пару не планируется повтор после достижения 400 суммарных наблюдений (последний пакет может пересечь 400).
3. Другой канал проверяется прямым пилотом. Публичные множители задают только оптимистичную оценку полезности проверки с учётом насыщения; итоговый эффект не переносится с одного канала на другой.
4. Остановка: нет доступного действия с потенциальной пользой выше расходов, альтернативной стоимости контактов и 1% текущей оценки плана; либо исчерпаны ресурсные/пилотные пределы. Это приближённая ценность информации, не точный байесовский расчёт.
5. Финальная оценка `mean * n/(n+20)` — слабое нейтральное сжатие. Четыре жадных распределения с разной ценой ресурсов сравниваются по дополнительной ожидаемой чистой выручке. Глобальная оптимальность не гарантирована. Порядок выбранных кампаний строится по текущей предельной пользе; весь план заранее помещается в остатки.
6. ID пилотных контактов недоступны. Используется верхнее приближение ожидаемого положительного эффекта уже охваченной доли аудитории, ограниченное лучшей наблюдаемой оценкой. Для неоднородных сегментов/повторных подвыборок оно неточно. Отрицательные пилоты не «отменяются» планировщиком.
7. До 3000 пилотных контактов и до 20% начального бюджета; резерв текущего финального плана. На маленьком остатке первый пилот может выйти за добровольный лимит разведки, сохранив физические лимиты и одну финальную кампанию. При полном отказе API возвращается пустой список: обещать валидный измеренный план в этом случае невозможно.
8. research отвечает за допустимые сегменты; аудитории вне 10–5000 не обрезаются. Контракт build_candidates v1 не изменён. LLM, секретов и новой платной инфраструктуры нет.

## Интеграция и сдача

При проверке открытых PR были только старые #1/#3. Они не интегрировались. PR ноутбуков 2/3 ещё не появились; общая командная сборка ожидает их проверки. Требуются повторные проверки и новый submission после интеграции.

Комплект должен содержать agent.py, strategy/, research/, data/change_tariff.csv, requirements.txt и submission.csv; для локального воспроизведения приложен полный отслеживаемый проект, включая неизменные материалы организатора. Открытые вопросы: официальное разрешение модульной упаковки и лимит 5/10 минут (docs/JUDGING.md).
