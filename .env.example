# Optional future LLM integration. The current baseline does not call APIs.
# Export these variables in your shell. This file is not loaded automatically.
OPENAI_API_KEY=
OPENAI_MODEL=
# Do not put coupon URLs or account/billing links in this repository.
