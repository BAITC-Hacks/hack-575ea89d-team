# Проверка обновлений frontend — 2026-09-23

Проверены main `93eac94` и frontend Манса `17e6319` (PR #2 слит в main). Backend/данные в этом обновлении не менялись относительно main `6de9d59`. Обновление объединено в `codex/final-integration` с исправлениями PR #3: единый origin, run.py, привязка анализа к инциденту, защита заказов от дублей и список заказов сохранены. Main и Brev этим прогоном не обновлялись.

## Автоматические проверки

- Опубликованный frontend main отдельно: 12 из 12 тестов прошли.
- Объединённая версия: 14 из 14 frontend-тестов и 39 из 39 backend-тестов прошли.
- `node --check frontend/app.js` и `git diff --check` прошли.
- Python 3.12, локальное изолированное окружение. SQLite тестов временная; реальные OpenAI-запросы не выполнялись. Предупреждение Starlette о будущем переходе TestClient с httpx на httpx2 не мешает проверкам.

## Реальный браузер + FastAPI

Локальный сервер 8767, данные Абыла (35 вышек/4200 жалоб), отдельная SQLite вне пользовательской базы. Подтверждено:

1. Dashboard загружается с того же origin, без отдельного frontend-сервера.
2. Leaflet загружает улицы OpenStreetMap, показывает 35 маркеров; работает приближение и выбор Tower 23.
3. Резервная схема показывает 35 вышек; переход из карточки вышки открывает INC-1045.
4. Анализ выбранного INC-1045 сохраняет этот ID и возвращает demo-режим.
5. Приоритет «Меньше нагрузка» выбирает для изучения доступное дополнительное оборудование (18 млн, 64%). Само исследование заказ не создаёт.
6. Вариант «Новая вышка» при бюджете 20 млн можно изучить; подтверждение заблокировано и показана нехватка 20 млн.
7. При бюджете 10 млн показана нехватка 2 млн до самого дешёвого решения; после возврата к 20 млн выполняется новый расчёт.
8. INC-1042 → модернизация из пульта → отдельное подтверждение → WO-8821 за 12 млн. Повтор возвращает тот же заказ и явное сообщение «без создания дубля».
9. При ширине viewport 390 px, с раскрытым сравнением и картой, горизонтального переполнения страницы нет (scrollWidth = clientWidth = 375 px с полосой прокрутки). После проверки viewport восстановлен.
10. Журнал браузера не содержит ошибок уровня error за проверенный сеанс.

## Границы результата

Карта требует интернет/CDN; fallback без библиотеки покрыт автоматическим тестом. Реальный OpenAI по-прежнему требует ключа и отдельного прогона. Локальные проверки не означают успешный GitHub Actions: в предыдущем прогоне задания были заблокированы биллингом аккаунта. Эта проверка не выполняет merge PR #3 и не заменяет текущую сборку на Brev.

## Повторная интеграция main 3a524b5 — 2026-09-23

Новый фронт добавляет appearance.js и меню lab-versus. Исправлена выдача нового JS через FastAPI (ранее 404). Проверены все JS/CSS ссылки HTML через HTTP: статус, Content-Type, Cache-Control. Теперь 40 backend и 14 frontend тестов проходят; оба JS проходят проверку синтаксиса. В браузере подтверждены переключение light/dark и сохранение после reload, выбор сравнения стрелками/Enter, корректная разница с новой вышкой (28 млн / 15 п.п.), demo INC-1042, заказ WO-8821 и возврат того же заказа при повторе. Использована отдельная SQLite. На 390 px обе темы без горизонтального переполнения; меню видимо и работает, ошибок консоли нет. Реальный OpenAI не вызывался.

После локального прогона runtime `6453cad` развёрнут на Brev. Сверка подтвердила отсутствие посторонних изменений кода относительно предыдущего релиза. Создан backup `network-backup-20260923-6453cad`, .env/.venv/SQLite сохранены. Все frontend-ресурсы отвечают 200, оба systemd-сервиса active, WO-8821 за 12 млн доступен после restart. Рабочая страница через порт 8000 подтверждает светлую/тёмную темы, пульт сравнений, улицы OSM и 35 маркеров; ошибок консоли нет. PR #3 открыт, main не изменён.
