# Текущее состояние проекта

Обновлено: 2026-09-23. Интеграция: `codex/final-integration`. В неё включены новый dashboard Манса, включая темы и меню сравнения из main `3a524b5`, данные/расчёты Абыла `6de9d59` и исправления Рамазана. Main автоматически не изменяется.

## Готово в интеграционной ветке

- Единый запуск `python run.py`, dashboard на `/`, API-документация на `/docs`; адрес API автоматически совпадает с адресом страницы. Отдельный frontend-сервер не нужен.
- Последние 35 вышек, 4200 жалоб и население 12 районов Абыла; его формулы и генератор сохранены. В 60-минутном демо-окне 464 жалобы, в том числе 347 для Tower 17. `demo_age_minutes` применяется только к явно синтетическим записям. Для обычных timestamp исключены будущие записи.
- Валидация формы JSON, существования/области/выбранного incident_id, выполненных инструментов, команды и приоритета. Ошибка провайдера/таймаут/невалидное решение даёт явный `demo_fallback`.
- Анализ привязан к выбранному инциденту; числа и назначения подтверждаются Python.
- Повторные/одновременные `/action` возвращают существующий pending-заказ; исторические дубли не удаляются. Список заказов доступен в API и UI.
- Создание инцидента по новому кластеру сериализовано, чтобы избежать гонки ID и дублей.
- GitHub Actions и автоматические тесты backend/frontend добавлены. После нового push Абыла прошли 39 backend-тестов и 11 frontend-тестов. Локальный браузер и реальный Brev подтвердили полный demo-путь и отсутствие повторного заказа.

## Реальные ограничения

- На Brev ранее подтверждены оба работающих systemd-сервиса, demo-анализ и сохранность WO-8821. На сервер развёрнут интеграционный код `6453cad`: новый dashboard с темами, картой и пультом, 35 вышек и 4200 жалоб. Прежний WO-8821 повторно прочитан после перезапуска; `/action` вернул его без дубля. Оба сервиса active.
- Ключ и модель OpenAI на сервере не заданы. Реальный платный API ещё не проверен; подставной клиент в тестах его не заменяет.
- Все данные и прогнозы синтетические; формулы и ограничения — [DEMO_MODEL.md](docs/DEMO_MODEL.md), [API_CONTRACT.md](docs/API_CONTRACT.md).
- Доступ к Brev через локальный SSH-туннель. Публичного URL/аутентификации нет; жизненный цикл полевых работ и история нагрузки не входят в MVP.
- Windows-путь запуска предусмотрен, но пока не проверен на Windows.

## Публикация и доступ

[PR #3](https://github.com/BAITC-Hacks/hack-575ea89d-team/pull/3) открыт, конфликтов с main нет, merge не выполнялся. Он уже включает frontend-коммиты PR #2. Для проверки командой: `git fetch origin`, `git switch --track origin/codex/final-integration` (если локальной ветки ещё нет), `python run.py`, затем http://127.0.0.1:8000/. При наличии незакоммиченных изменений сначала сохраните их; не используйте принудительное переключение.

Brev содержит архив кода `6453cad`, без .git. Свежая резервная копия предыдущего кода и SQLite: `/home/ubuntu/workspace/network-backup-20260923-6453cad`. `.env` сверён до/после без вывода содержимого и не изменён; `.venv` сохранён. На сервере подтвердили прогноз 71%, улучшение для 600 пользователей и сохранность WO-8821 после перезапуска.

## Внешние блокеры

1. Ввести ключ и модель OpenAI через `deploy/brev/configure-openai.py` в SSH-терминале, перезапустить backend и выполнить `verify-ai.py --require-openai`. Пока проверен только demo и подставной OpenAI в тестах.
2. GitHub Actions не начал ни одного шага: аннотация запуска 35850257724 — `The job was not started because your account is locked due to a billing issue.` Workflow добавлен корректно, но зелёного CI нет. Владелец аккаунта/организации должен решить биллинг и повторить проверки. Настройки оплаты не изменялись.
3. Согласовать merge PR #3. Публичный адрес не настраивался; Brev доступен через SSH-туннель. Для доверенной LAN локальный запуск поддерживает `--host 0.0.0.0`.


## Проверка свежего frontend

Обновление main 93eac94 / Mans 17e6319 включено в интеграционную ветку: карта улиц Астаны и Decision Studio сохранены вместе с исправлениями PR #3. Пройдены 39 backend + 14 frontend тестов; отдельно исходный новый frontend main прошёл все 12 тестов. Браузер подтвердил 35 маркеров, карту/схему, приоритеты и ограничения бюджета, анализ выбранного инцидента, создание/повтор заказа и узкий экран. Ошибок браузерного журнала нет. Подробности: [FRONTEND_VALIDATION.md](docs/FRONTEND_VALIDATION.md).

PR #3 обновляется для проверки новой связки. Main и Brev не менялись в этом прогоне: на Brev остаётся ранее проверенный runtime 0c8fe0f (без новой карты/пульта). Пользователь уточнил, что сообщение «3» было случайным; разрешения на merge оно не давало.


## Светлая и тёмная темы — 2026-09-23

По уточнению пользователя палитры операторов удалены. Оставлены только светлая и тёмная темы с исходными акцентами dashboard. Переключатель в блоке «Оформление» сохраняет режим в network-dashboard:appearance:v1; старое поле palette игнорируется и удаляется при загрузке. Изменены frontend/index.html, frontend/styles.css, frontend/appearance.js. API и запуск прежние. Публикация в main поверх 93eac94 по запросу пользователя: светлая/тёмная темы и оформление меню сравнения. Проверены обе темы в браузере, синтаксис JavaScript и 12 регрессионных тестов. Следующей сессии проверить актуальный main перед изменениями. Данные и прогнозы остаются демонстрационными.



## Интеграция тем и меню сценариев — 2026-09-23

Main `3a524b5` включён в PR #3. Добавлена выдача `/appearance.js` через FastAPI: без этого новый переключатель тем получал 404 при едином запуске. Регрессионная проверка теперь запрашивает все локальные JS/CSS, указанные в HTML, и проверяет тип ответа и отсутствие кеширования. Сохранены единый origin, выбранный инцидент и защита заказов от дублей. Прошли 40 backend + 14 frontend тестов; браузер подтвердил сохранение темы после reload, сравнение мышью/клавиатурой, demo-анализ, создание и повтор заказа, обе темы на 390 px без переполнения и ошибок консоли. Код `6453cad` развёрнут на Brev после сверки файлов с прежней версией. Резервная копия создана, .env/.venv/SQLite сохранены. Сервер подтвердил 200 для всех frontend-ресурсов, 35 вышек и сохранность WO-8821 за 12 млн. Оба сервиса active. Браузер через рабочий туннель подтвердил обе темы, Decision Studio и 35 маркеров на загруженной карте OSM, ошибок консоли нет. PR #3 обновлён; main не изменён.
