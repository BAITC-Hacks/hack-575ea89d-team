# Ноутбук 2 — твой другой аккаунт: данные и гипотезы

## Контекст

Ты работаешь на втором ноутбуке того же владельца. Основной ноутбук пишет агента и собирает проект, напарник делает проверки/демо. Репозиторий BAITC-Hacks/hack-575ea89d-team теперь решает кейс Beeline Tariff Marketing Campaigns, а не старую задачу про вышки.

Нужно выбирать прибыльные тарифные кампании для 23 441 абонента. Агент проверяет гипотезы шумными пилотами, затем возвращает 1–10 кампаний. 100 000 у.е. и 15 000 контактов включают пилоты; 5000 абонентов на кампанию; 20 пилотов по 10–200. История описывает другую выборку. Твоя роль — предложить хорошие гипотезы для тестирования, а не угадать скрытые ответы.

## Начало

Прочитать AGENTS.md, STATUS.md, README.md, PARTICIPANT_GUIDE.md, docs/API_CONTRACT.md. Проверить чистоту своей копии и актуальный main. Начать от актуального main нового проекта:

```bash
git fetch origin
git switch -c work/laptop-2-research origin/main
```

Существующую ветку продолжить, не перезаписывать. Поднять окружение по README. Начать с research/candidates.py и заголовков CSV.

## Только твоя зона

`research/`, `docs/RESEARCH.md`, `tests/test_research*.py`, этот README. Не менять agent.py, strategy/, общий requirements, исходные CSV/скоринг и чужую документацию. Если нужна новая зависимость — предложить ноутбуку 1; стараться использовать имеющиеся pandas/numpy.

## Конкретные задачи

1. Разобрать customer_profile, историю смен тарифов, traffic, arpu_monthly, справочники. Проверить ключи, пропуски, дубликаты, распределение размеров сегментов, наличие истории по парам тарифов. Исходные данные не менять.
2. Улучшить `build_candidates(profile, tariffs, history_path=None) -> list[dict]` по контракту. Использовать историю, текущую ценность/размер аудитории, потребление и параметры тарифов. Не вызывать env.run_pilot: это зона ноутбука 1.
3. Учитывать малые исторические выборки, выбросы и отсутствие истории. Не тратить все гипотезы на один сегмент. Подумать о разбиении сегментов >5000 разрешёнными data/call-фильтрами.
4. Явно разделять историческое изменение ARPU среди перешедших и ожидаемый эффект кампании. История смен не даёт знаменатель для вероятности отклика.
5. В docs/RESEARCH.md записать 5–10 обоснованных гипотез, способ ранжирования, ограничения и что агент должен проверить пилотами. Все числа вычислять из данных.
6. Проверить детерминизм, отсутствие изменения входных DataFrame, валидность тарифов/фильтров, непустые сегменты и корректность на редкой/отсутствующей истории. Сравнить local_eval до/после на тех же seed, пометить результаты как mock.

Ни LLM, ни GPU для этой части не обязательны. Не активировать кредиты и не вшивать ссылки на купоны/ключи. Не подгонять код к внутренней таблице мока.

## Результат для основного ноутбука

Открыть PR в main с изменениями только своей зоны. Описать контракт, наблюдаемые улучшения/ухудшения, проверки, ограничения. Не объединять PR самостоятельно. Общие README/STATUS обновит ноутбук 1, ты обновляешь этот README. При конфликте интерфейсов сначала предложить конкретное изменение контракта.

## Стартовый статус — 2026-09-23

Уже есть простой генератор кандидатов: усреднённый относительный прирост, уменьшение веса малых групп, два варианта на сегмент с проходом по разным сегментам. Следующий шаг: EDA и более качественные гипотезы без изменения интерфейса.

После каждой сессии: дата, ветка/коммит, изменённые файлы, фактические проверки, следующий шаг и блокеры.

## Выполнено — 2026-09-23

Ветка: `work/laptop-2-research`, основа `main` / `58c794b`. Локальная ранее существовавшая ветка продолжена fast-forward, без reset/force-push. Чужих незакоммиченных изменений не было. Рабочая копия этой сессии отделена от предыдущей.

Изменённые файлы:

- `research/candidates.py`: очистка истории, робастное сглаживание и штраф за неопределённость, учёт потребления/пакетов/ценности аудитории, cold start и разрешённое разбиение больших ячеек. Сигнатура и поля контракта v1 сохранены, входы не изменяются.
- `research/audit.py`: воспроизводимый аудит всех семи CSV и чисел первых десяти гипотез; запуск `python -m research.audit`.
- `tests/test_research_candidates.py`: 12 тестов контракта, устойчивости и крайних случаев без обращения к среде.
- `docs/RESEARCH.md`: источники, качество данных, метод, 10 гипотез, ограничения и mock-сравнение по seed.
- Этот README: статус и передача интегратору.

Проверено на Windows, Python 3.12.14, numpy 2.5.3, pandas 2.3.3 (готовое изолированное окружение с версиями из requirements):

- До изменений: `python local_eval.py` PASS; `--runs 10` — 10/10 положительных, медиана net 3 178 575.
- После: те же команды, 10/10 положительных, медиана net 3 384 892. Seed 42: 2 519 731 → 2 678 813; seed 9 ухудшился: 2 496 122 → 2 191 729. Это mock, не гарантия скрытого результата. Параметры по этому сравнению не подбирались.
- Исследовательские тесты: 12/12 PASS. Полный набор в Windows-checkout: 18/20 PASS; baseline был 6/8. Причины: преобразование исходных LF в CRLF при checkout, Windows-различие to_csv/read_text и устаревший submission после изменения кандидатов.
- В отдельной копии исходных Git-байтов без CRLF-преобразования: baseline 7/8, после 19/20 PASS; единственная оставшаяся ошибка — сравнение с сохранённым submission. Проверка воспроизводимости двух генераций внутри этого теста проходит.
- Все 15 исходных Git blobs совпадают с SHA256 организаторов. Исходные файлы в рабочей копии отличаются от blobs только окончаниями строк; их содержимое не редактировалось. `git diff --check` PASS.

Важные ограничения: история не пересекается с целевой аудиторией; support — события среди сменивших тариф, не получатели предложений. В месячной выручке неоднозначные дубли ID–месяц; LTE-трафик противоречит общему. Исторический prior и priority не являются ожидаемой прибылью. В топ-10 есть cold-start `tariff_8/HIGH → tariff_16` с support=0 и prior=0: нужен пилот и проверка риска downsell. Малые LOW-выборки также требуют осторожности. На реальных CSV нет ячеек >5000; ветка разбиения проверена синтетически.

Следующие шаги ноутбука 1: рассмотреть PR, подключение уже существующее (`build_candidates`), проверить гипотезы своей логикой пилотов, затем пересоздать финальный `submission.csv` и повторить общие проверки. Контракт и зависимости менять не требуется. После интеграции обновить общий STATUS. Ноутбуку 3: сравнивать CSV независимо от LF/CRLF и использовать исходные LF-байты для манифеста. Исследовательский PR не редактирует agent/strategy, тесты соседей, материалы организаторов и submission; слияние не выполняется.

### Итог объединения параллельной работы

При push обнаружен параллельный commit `3714cf9` и существующий draft PR #5. Его история сохранена обычным merge с текущим `8819492`, без force-push. Обновляется [PR #5](https://github.com/BAITC-Hacks/hack-575ea89d-team/pull/5), новый дублирующий PR не создаётся.

Дополнительно сохранены `research/median_candidates.py`, `median_audit.py`, `median_audit.json`, `median_mock_comparison.json`, `median_RESEARCH.md`; это отдельный медианный эксперимент, не подключённый к агенту. Его тесты `tests/test_research.py` проверяют именно медианную версию. Сохранён и расширен парный benchmark `tests/test_research_benchmark.py` (выбор `--variant current|median`). `research/audit.json` и `research/mock_comparison.json` обновлены под текущий метод. Защита от некорректного predicted_arpu перенесена в текущую версию: исключается целая ячейка.

Финальное состояние: 21/21 research-тестов PASS; полная suite в копии с исходными LF — 28/29 PASS, ожидаемая ошибка сохранённого submission остаётся за интеграцией. Повторённый парный benchmark seed 0–9 и 42 дал те же текущие результаты, что указаны выше. JSON-аудиты обоих вариантов воспроизводятся. Текущий контракт подключения не изменился. PR не объединён.

## Устойчивость и воспроизводимость — main 6dad30c, 2026-09-23

PR #5 / `9b7d5ef` уже интегрирован через PR #8. Рабочая ветка обновлена fast-forward до `6dad30c`, локальных незакоммиченных изменений не было. Записи выше — архив прошлой сессии.

Протокол **до результатов**: commit `80b5fb6`, `research/experiments/stability_6dad30c/plan.json`. Неизменный текущий planner сравнивался с тремя генераторами (исходный из `58c794b`, текущий, median) на seed 0–9, 30–39, заранее объявленных 50–69. Генераторы/agent/strategy не менялись и параметры по результатам не подбирались.

Изменения:

- `tests/test_research_benchmark.py`: режим явно candidate_generators_fixed_planner, реальные source snapshots/hash/commit, хеши CSV/evaluator/requirements, версии Python/numpy/pandas, список seed и порядок запусков; сырые результаты и все проигрыши; запрет перезаписи файлов.
- `research/provenance.py`: сбор fingerprints (сырые и LF), фиксация изменения файлов во время эксперимента, exclusive-create JSON.
- `research/stability.py`: 200 bootstrap повторов событий с сохранением кратности, оценка первых 12, частот/рангов/альтернатив, 94 удаления одного события из шести малых ячеек. Только публичная история, без среды.
- `tests/test_research_evidence.py`, `tests/test_research_stability.py`: 9 новых тестов корректности исследования.
- `tests/test_research_replay.py`: повтор сохранённого seed с отказом при изменении planner/данных/зависимостей. Повтор seed 50 совпал для всех трёх генераторов.
- Новые JSON в `research/experiments/stability_6dad30c/`: `generators.json`, `history_stability.json`, `replay_seed50.json`, `historical_index.json`, протокол. Четыре старых JSON research сохранены без изменения; общий полный агент не сравнивается под видом смены одного генератора.
- `docs/RESEARCH.md`: актуальные таблицы трёх серий, частоты top-12, анализ отдельных наблюдений, девять конкретных рекомендаций. Старый отчёт оставлен отдельным архивным разделом.

Результаты mock при фиксированном planner:

- Seed 0–9: текущий против исходного — 6 улучшений / 4 ухудшения, средняя разница +92 391 у.е.
- Seed 30–39: 7 / 3, средняя разница +146 075.
- Seed 50–69: 13 / 7, средняя разница +245 658; median net 3 817 754 против 3 587 585. Проигрыши: 50, 51, 54, 55, 59, 60, 65; худшая разница −471 192. Против median — 12 / 8, поэтому доминирования нет.
- Это другой baseline, чем прежние сравнения полных агентов в AGENT_VALIDATION. Все 120 запусков успешны, но не доказывают перенос на скрытое судейство. Контрольные seed теперь просмотрены.

Ранжирование: средний Jaccard top-12 = 0,531. `tariff_8/LOW → tariff_9` появляется в top-12 200/200 раз, `tariff_4/LOW → tariff_9` — 193/200; это не уверенность в причинном эффекте. `tariff_10/LOW → tariff_9` теряет top-12 при 6/8 удалениях, `tariff_14/MID → tariff_9` при 5/7. `tariff_4/MID` требует готовности проверить альтернативы tariff_8 и tariff_9. Cold-start `tariff_8/HIGH → tariff_16` по-прежнему имеет нулевую историю.

Проверено: 200 bootstrap + 94 leave-one-out; парные 120 запусков; replay seed 50; официальный local_eval seed 42 (3 740 829) и --runs 10 (median 3 760 624); 38/38 общих тестов в отдельной LF-копии с os.linesep=chr(10) только в тестовом процессе, отдельно 6/6 strategy. Обычная Windows-suite: 36/38 из-за прежних CRLF/SHA256 и to_csv/read_text. В LF-копии существующий submission совпадает с агентом; его не генерировали и не редактировали. Исходные хеши проходят.

Следующий шаг ноутбука 1: прочитать девять рекомендаций в начале RESEARCH.md, сохранить ресурсы для слабых/конкурирующих гипотез в своей логике пилотов. Автоматической смены действующего генератора нет; контракт v1 и зависимости прежние. Новые цифры использовать с их manifests, старые серии не перезаписывать. Формально положительный mock и устойчивая позиция в bootstrap не заменяют наблюдения пилота. Новый PR направляется в main без слияния.
